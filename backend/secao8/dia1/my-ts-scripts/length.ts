const convert = (valor: number, base: string, _conversao: string) => {
  let tabela = {km: 0, hm: 0, dam: 0, m: 0, dm: 0, cm: 0, mm: 0};
  switch (base) {
    case 'km':
      
      break;
      
    default:
      break;
  }
}

convert(5, 'm', 'km');